<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found | Footzy</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #FF6B00;
            --secondary-color: #333333;
        }

        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            overflow-x: hidden;
        }

        /* Updated navbar styles */
        .navbar {
            padding: 1rem 0;
        }

        .navbar-brand {
            margin: 0;
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
        }

        .navbar-brand img {
            height: 40px;
            width: auto;
            max-width: 160px;
            object-fit: contain;
        }

        /* Media queries for responsive logo */
        @media (max-width: 576px) {
            .navbar-brand img {
                height: 32px;
                max-width: 130px;
            }
        }

        /* Rest of your existing styles... */
        .error-section {
            flex: 1;
            background: var(--primary-color);
            position: relative;
            overflow: hidden;
            min-height: calc(100vh - 76px);
            display: flex;
            align-items: center;
        }

        .error-number {
            font-size: 180px;
            font-weight: bold;
            color: rgba(255, 255, 255, 0.15);
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            z-index: 1;
            user-select: none;
            pointer-events: none;
            white-space: nowrap;
        }

        .error-content {
            position: relative;
            z-index: 2;
            padding: 2rem;
            color: white;
            text-align: center;
            max-width: 800px;
            margin: 0 auto;
        }

        .btn-primary {
            background-color: white;
            border-color: white;
            color: var(--primary-color);
            padding: 12px 30px;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: transform 0.3s;
        }

        .btn-primary:hover {
            background-color: rgba(255, 255, 255, 0.9);
            border-color: white;
            color: var(--primary-color);
            transform: translateY(-2px);
        }

        .btn-outline-dark {
            background-color: transparent;
            border-color: white;
            color: white;
        }

        .btn-outline-dark:hover {
            background-color: white;
            border-color: white;
            color: var(--primary-color);
        }

        .animated-background {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            overflow: hidden;
            z-index: 1;
        }

        .circle {
            position: absolute;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            animation: float 20s infinite linear;
        }

        .circle:nth-child(1) {
            width: 300px;
            height: 300px;
            left: -150px;
            top: -150px;
            animation-delay: -5s;
        }

        .circle:nth-child(2) {
            width: 500px;
            height: 500px;
            right: -250px;
            bottom: -250px;
            animation-delay: -10s;
        }

        .circle:nth-child(3) {
            width: 200px;
            height: 200px;
            right: 20%;
            top: 20%;
            animation-delay: -15s;
        }

        @keyframes float {
            0% {
                transform: rotate(0deg) translate(50px) rotate(0deg);
            }
            100% {
                transform: rotate(360deg) translate(50px) rotate(-360deg);
            }
        }

        @media (max-width: 991.98px) {
            .error-number {
                font-size: 120px;
            }

            .error-content {
                padding: 2rem 1rem;
            }
        }

        @media (max-width: 576px) {
            .error-number {
                font-size: 100px;
            }

            .error-content {
                padding: 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Updated Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container position-relative">
            <a class="navbar-brand" href="index.html">
                <img src="/img/logo.png" alt="Footzy Logo">
            </a>
        </div>
    </nav>

    <!-- 404 Error Section -->
    <section class="error-section">
        <div class="animated-background">
            <div class="circle"></div>
            <div class="circle"></div>
            <div class="circle"></div>
        </div>
        <div class="error-number">404</div>
        <div class="container">
            <div class="error-content">
                <h1 class="display-1 fw-bold mb-4">Oops!</h1>
                <h2 class="display-6 mb-4">Looks like this page ran away</h2>
                <p class="lead mb-5">The page you're looking for might have been removed, had its name changed, or is temporarily unavailable.</p>
                <div class="d-flex gap-3 justify-content-center">
                    <button onclick="gotoHome()" class="btn btn-primary btn-lg">
                        Back to Home
                    </button>
                    <button class="btn btn-outline-dark btn-lg">
                        View Collection
                    </button>
                </div>
            </div>
        </div>
    </section>

<script>
    
function gotoHome(){
    window.location.href = '/'
}

</script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>