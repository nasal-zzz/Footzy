<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.linearicons.com/free/1.0.0/icon-font.min.css">

    <style>

.add-address {
            text-align: center;
            margin: 20px 0;
        }
        .add-address button {
            background-color: #f57c00;
            color: #fff;
            border: none;
            padding: 12px 20px;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
        }
        .add-address button:hover {
            background-color: #e65100;
        }
        .default-badge {
            background-color: #f57c00;
            color: #fff;
            padding: 5px 10px;
            border-radius: 4px;
            font-size: 12px;
            margin-left: 10px;
        }
        .form-container {
            margin: 20px auto;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 15px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
        .form-container input {
            width: 98%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        .form-container button {
            background-color: #f57c00;
            color: #fff;
            border: none;
            padding: 12px 20px;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
        }
        .form-container button:hover {
            background-color: #e65100;
        }

    .error {
    color: red;
    font-size: 0.8em;
}
#addressType{
    width: 98%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ddd;
            border-radius: 4px;
            }

            #brodcrumb{
            margin-left:85%;
            margin-top: 65px;
        }
        #brodcrumb >a{
            text-decoration:none;
            color: #f57c00;
        }
        #nav-main{
            height: 80px;
        }
        #addAddressForm{
            margin-top: 77px;
        }

    </style>
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">
     <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
 

</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-light bg-white  fixed-top" id="nav-main">
        <div class="container">
					<a class="navbar-brand logo_h" href="/"><img src="/img/logo.png" alt=""></a>
            <div class="d-flex">
                <a href="/" class="btn btn-outline-danger me-2 text-decoration-none">Home</a>
                <a href="cart.html" class="btn btn-outline-danger">
                    <i class="fas fa-shopping-cart"></i>
                </a>
                
            </div>
        </div>
    </nav>

    <nav class="d-flex align-items-end ml-4 fixed-top " id="brodcrumb">
        <a href="/address">Address<span class="lnr lnr-arrow-right"></span></a> 
        <a href="#">Edit Address</a>
    </nav>


    <div class="form-container " id="addAddressForm">
        <h2>Edit Address</h2>
        <form id="addressForm" action="/editAddress" method="post">
            <label for="fullName">Full Name:</label>
            <input type="text" id="fullName" name="fullName" placeholder="Full Name" value="<%= data.fullName %>">
            <span class="error" id="fullNameError"></span> <!-- Error message for full name -->
            <br>

            <input type="text" name="index" hidden value="<%= index %>">
        
            <label for="address">Address:</label>
            <input type="text" id="address" name="address" placeholder="E.g house name" value="<%= data.address %>">
            <span class="error" id="addressError"></span> <!-- Error message for address -->
            <br>
            <label for="addressType">Address Type:</label>              
             <br>

            <select id="addressType" name="addressType" class=""  >
            <option value="Home">Home</option>
            <option value="Work">Work</option>
            <option value="Office">Office</option>
            </select>
            <br>
            <br>

            <label for="country">Country:</label>
            <input type="text" id="country" name="country" placeholder="Country" value="<%= data.country %>">
            <span class="error" id="countryError"></span> <!-- Error message for country -->
            <br>
      
        
            <label for="state">State:</label>
            <input type="text" id="state" name="state" placeholder="State" value="<%= data.state %>">
            <span class="error" id="stateError"></span> <!-- Error message for state -->
            <br>
    
            <label for="city">City:</label>
            <input type="text" id="city" name="city" placeholder="City" value="<%= data.city %>">
            <span class="error" id="cityError"></span> <!-- Error message for city -->
            <br>

            <label for="place">Place:</label>
            <input type="text" id="place" name="place" placeholder="Place" value="<%= data.place %>">
            <span class="error" id="placeError"></span> <!-- Error message for city -->
            <br>


            <label for="pinCode">Pin Code:</label>
            <input type="text" id="pinCode" name="pincode" placeholder="Pin Code" value="<%= data.pincode %>">
            <span class="error" id="pinCodeError"></span> <!-- Error message for pin code -->
            <br>
        
            <label for="phoneNumber">Phone Number:</label>
            <input type="tel" id="phoneNumber" name="phone" placeholder="Phone Number" value="<%= data.phone %>">
            <span class="error" id="phoneNumberError"></span> <!-- Error message for phone number -->
            <br>
        
            <label for="landmark">Landmark:</label>
            <input type="text" id="landmark" name="landMark" placeholder="E.g near st Marry's school" value="<%= data.landMark %>">
            <span class="error" id="landmarkError"></span> <!-- Error message for landmark -->
            <br>
        
            <button type="submit">Edit Address</button>
        </form>
    </div>
    
<script>


document.getElementById('addressForm').addEventListener('submit', function(event) {
            // Prevent form submission

            const fullName = document.getElementById('fullName').value.trim();
            const address = document.getElementById('address').value.trim();
            const country = document.getElementById('country').value.trim();
            const city = document.getElementById('city').value.trim();
            const state = document.getElementById('state').value.trim();
            const place = document.getElementById('place').value.trim();
            const pinCode = document.getElementById('pinCode').value.trim();
            const phoneNumber = document.getElementById('phoneNumber').value.trim();
            const landmark = document.getElementById('landmark').value.trim();


            // Clear previous error messages
            const errorMessages = document.querySelectorAll('.error');
            errorMessages.forEach(function(error) {
                error.textContent = ''; // Clear the error message text
            });

            // Validate Full Name
            if (fullName === '' || !fullName.match(/^[A-Za-z\s]+$/)) {
                event.preventDefault();

                showError('fullNameError', 'Please Enter a valid Name..!');
            }else
            if (address === '') {
                event.preventDefault();

                showError('addressError', 'Address is required.');
            }else
            if (country === '') {
                event.preventDefault();

                showError('countryError', 'Country is required.');
                
            }else
            if (city === '') {
                event.preventDefault();

                showError('cityError', 'City is required.');
            }else
            if (place === '') {
                event.preventDefault();

                showError('placeError', 'Place is required.');
            }else
            if (state === '') {
                event.preventDefault();

                showError('stateError', 'State is required.');
            }else
            if (!/^\d{6}$/.test(pinCode)) {
                event.preventDefault();

                showError('pinCodeError', 'Pin Code must be a 6-digit number.');
            }else
            if (!/^\d{10}$/.test(phoneNumber)) {
                event.preventDefault();

                showError('phoneNumberError', 'Phone Number must be a 10-digit number.');
            }else
            if (landmark === '') {
                event.preventDefault();

                showError('landmarkError', 'Landmark is required.');
            }else{
                event.preventDefault(); // Prevent default to show Swal
        Swal.fire({
            title: `Address Edited successfully..!`, 
            icon: 'success',
            showCancelButton: false,
            showConfirmButton: false,
            timer: 2500,
        }).then(() => {
            document.getElementById('addressForm').submit(); // Submit the form programmatically
        });
            }

           
        });

        function showError(errorId, message) {
            const errorElement = document.getElementById(errorId);
            errorElement.textContent = message;
        }


</script>

</body>
</html>